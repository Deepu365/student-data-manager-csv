Title:
"Student Data Mnager (csv-based)" is a oython project
    That stores student Details(name,age,marks)
    Reads and display stored data
    
 Works:
    student datais defined inside the code
    python writes data into a csv file
    Display all stored student records
    
Technologies used:
    Python
    CSV
    OOPS
    
